# OSMesa joint0 candidate qualification

Review profile: `DUAL_REVIEW`. Evidence class: `PUBLIC_REPOSITORY_ONLY`. Phase-gate effect: `NONE`.

This is a candidate-only, failure-preserving development qualification. It never claims qualification, adoption, gate passage, positive rendered split/merge, or all-pixel numeric alignment. Its terminal completed status is `PARTIAL_EVIDENCE_ALIGNMENT_UNSPECIFIED`.

The immutable order is single_occluder base repeat0/repeat1, alternate repeat0/repeat1, then corridor in the same order. Every attempt calls `generate_dataset(config, episodes=4, component_topology=False)` with seed 1729, at 160x120. It reserves before creating output and a reservation, failure, source/plan/runtime mutation, identity, permission, repeat, or appearance failure permanently stops the namespace.

There are 32 contexts and 64 ordinary before/after poses. Each ordinary pose has RGB, depth, and segmentation: 192 ordinary SDK modalities. Single-occluder retains its canonical counterfactual segmentation render per pose, so the exact native `mjr_render`/`mjr_readPixels` total is 224: 4 single attempts × 32 plus 4 corridor attempts × 24. Counterfactual events bind scene-option/geomgroup identity and are labelled separately.

Immediately before exactly one original `mujoco.Renderer` constructor per context, the scoped adapter sets only `model.vis.quality.offsamples=0`. It rejects replacements, reentrancy, wrong thread, and double construction, checks OSMesa/Linux, actual context, zero sample attachment/FBO facts, dimensions and GL error status before the first render, and restores the original class in `finally`.

The receipt binds clean source HEAD/tree, lock, configs, profile/registry and source hashes, package/binary/SDK hashes, complete output manifest/file hashes, context observations, original native call counts, and restoration flags. Dataset provenance alone is not zero-sample evidence.

Repeat comparison is exact over deterministic nonvolatile artifacts, shapes, dtypes and bytes, with only relative `run.json` excluded. Appearance comparison predeclares all public ecological views/actions and target arrays plus canonical semantic geometry, camera trajectory, action and logical-surface/remap correspondence. RGB needs an actual difference to claim exercised contrast; RGB and depth are not ecological invariance targets. Permission probes exercise positive ecological accesses and metric depth, then deny depth, camera, raw IDs/world coordinates, geometry, generation records, remapping/appearance controls and raw transition metadata before protected open where observable.

Analytic centre-ID/depth diagnostics use reviewed `revision_analysis` controlled-hit interior/boundary/nohit strata only. They use no fitted threshold and do not establish all-pixel numeric alignment. Validation is non-rendering; PNG inspection occurs afterward and is CPU-only.
