# PR #21 independent scientific review

**Review request ID:** `eps-slice6-ordinary-pr21-scientific-20260913-32f780dc`  
**Role:** Independent scientific reviewer; no implementation, coordination, owner, closeout, or empirical-gate authority  
**Review profile:** `SCIENTIFIC_ONLY`  
**Evidence class:** `PUBLIC_REPOSITORY_ONLY`  
**Repository:** `yurifrusin/ecological-predictive-states`  
**Pull request:** #21, `Slice 6: ordinary-development diagnosis and bounded continuation`  
**Exact reviewed head:** `32f780dc101030f8c34f819be5d9aea3d9433f69`  
**Exact reviewed tree:** `4098b535058cccc74ebb7d79fe652c8c0756ca80`  
**Base:** `main` at implementation merge `f8a1b8584889fb955085517a7b51a4ad9a63ade9`, tree `8ef8f5552750e322c01538fdf7be30a1855c0a63`  
**Signed-by-role:** Independent scientific reviewer  
**Date:** 2026-09-13

## Exact-head and scope verification

I verified live connected-GitHub metadata for draft PR #21 and the clean review worktree at `C:\Users\sarashera\EPS-worktrees\slice6-ordinary-development-memo`. The remote branch and worktree both resolve to the exact reviewed head above. The three-dot diff from the stated base adds exactly one file, `docs/development/2026-09-13-slice6-ordinary-diagnosis.md`, with 90 insertions and no deletions. There is no other source, test, dependency, configuration, benchmark, workflow, lock, generated artifact, or historical review-record change.

The added Git blob is `f212ce26218d8181d5b500ff7ab7b07f308d4014`; its exact committed content is 12,487 bytes with SHA-256 `fd1aef6d7da7223b5c700c05a8030f73c9d237e9dbde0c3731434c0856a01fe8`. The clean Windows checkout has CRLF line endings, so its filesystem byte hash differs; the reviewed Git blob matches the requested identity. `git diff --check` passes.

PR #21 is open, draft, and unmerged. CI run `34731149092` is completed with conclusion `cancelled` at this exact head. Lint, formatting, type checking, and the freeze-documentation check completed successfully before cancellation; the test step was cancelled and renderer qualification did not run. I do not treat this as a successful CI run and do not rely on it for the disposition.

## Evidence examined

I read the exact memorandum and PR body, applicable `AGENTS.md` and `docs/review-protocol.md` rules, the unchanged configuration and scene-building code relevant to the memorandum's bounded feasibility claim, and the compact ordinary-development evidence retained outside the repository. I recomputed and matched all eight file identities in `evidence-index.json`: both prospective ledgers, both generation receipts, the summary, diagnostics, array/isolate trace, and comparison figure.

This review reuses the immediately preceding independent read-only examination of the same byte-identical ordinary evidence. That examination validated all four two-episode datasets, reproduced the component/status table from saved arrays, checked raw/public difference-mask equality, checked equality of all six public transport arrays and both event-code arrays for all four paired transitions, audited difference morphology, verified ordinary/final membership disjointness, and validated the proposed numeric configuration and exact reverse without rendering. I did not repeat generation or the broad implementation test/qualification campaign because this PR changes only the memorandum and all evidence identities remain unchanged.

## Scientific assessment

The memorandum accurately reports the bounded result. Both ordinary single-occluder pairs have identical segmentation and portable topology across WGL and OSMesa. Both corridor pairs first differ in retained raw segmentation labels; public remapping exposes differences at exactly the same coordinates, while the retained transport and event-code arrays agree elementwise. The WGL-only components have no supported incident graph edge and no valid outgoing transport pixel. The frozen conservative method therefore retains the components and absent support and reports indeterminacy. This supports a segmentation-output-first localization within the observed pipeline, not a graphics-level causal diagnosis.

The interpretation limits are explicit and adequate:

- the four paired transitions are a finite diagnostic sample, not a renderer-wide rate estimate;
- the two single-occluder episodes do not add geometric coverage;
- sample resolution and geometry-ID encoding/decoding are candidate mechanisms within a broader segmentation production/readback path, not exhaustive or established causes;
- neither renderer is declared ground truth, and small or thin components are not filtered after seeing the negative result;
- the failed frozen qualification remains `FAILED_CLOSED`, with no change to topology, support, renderer acceptance, or the old final matrix;
- existing closed-form fixtures establish analytic split/merge/reversal semantics, while the frozen and ordinary renders contain no positive rendered split, merge, or complex event.

The proposed single-occluder pair is reported at the correct evidential level. Strict validation accepts camera lateral `-1.0 → 0.0`, forward position `0.0`, height `1.25`, vertical field of view `90°`, and lateral-right delta `+1.0`; its exact reverse swaps the endpoints and uses lateral-left delta `-1.0`. Analytic projection makes a background split and reverse merge geometrically plausible, but the memorandum does not claim a rasterized component change or sufficient directional support. It prospectively records the candidate, requires retention of a negative outcome, and prohibits seed/geometry tuning into the old matrix.

The next diagnostic is finite and falsifiable: audit already generated corridor episode-0 differing coordinates against scene seams, then inspect the installed renderer segmentation path before selecting any one intervention. Any intervention would require a separate prospective protocol, both fixed backends, an explicit baseline, and a new review. This preserves comparison fairness and prevents the development result from silently changing the scientific contract.

The ordinary inputs are generated from repository configurations and deterministic seed derivation and contain no hidden benchmark membership or private learned labels. Raw simulator labels, role names, camera records, and generation details remain diagnostic/privileged instrumentation and are not introduced into ecological-only learner inputs. Keeping generated ordinary artifacts untracked is consistent with repository guidance; the memorandum supplies their identities, provenance, reproducibility route, and absolute retention location without treating them as gate evidence.

## Findings

No material scientific findings.

## Disposition

**SCIENTIFIC_PASS**

This disposition approves the scientific accuracy, interpretation boundaries, and bounded continuation language of the one-file non-executable memorandum at the exact PR/head named above. It does not modify or renew the PR #19 review, authorize execution of the proposed intervention or split/merge candidate, approve a renderer or method revision, authorize merge, or supply owner/PI or closeout approval.

**Phase-gate effect: NONE**

There is no Gate 0B advancement and no Gate 0C, Gate 0D, model-efficacy, or empirical authority.
